# Server Docker

Builds an image that contains only the Node.js WebSocket server.

## Usage
From `server/docker`:
```
docker compose up --build
```

Server will be available at `ws://localhost:8080`.
